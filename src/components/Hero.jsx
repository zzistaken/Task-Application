function Hero() {
  return (
    <section className="hero is-small is-link">
      <div className="hero-body">
        <p className="title">Task Application</p>
        <p className="subtitle">
          Application for creating/deleting and editing tasks via a form.
          Created in React + Vite, styled with Bulma CSS.
        </p>
      </div>
    </section>
  );
}

export default Hero;
